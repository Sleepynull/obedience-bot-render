services:
  - type: worker
    name: obedience-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    disk:
      name: obedience-data
      mountPath: /data
      sizeGB: 1
